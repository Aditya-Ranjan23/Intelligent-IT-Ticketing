/**
 * Legacy Node API (Cursor SDK deep resolution).
 * Primary ML service: `python -m uvicorn api.main:app --port 8000`
 */
console.error(
  "[deprecated] Use the Python ML API: pip install -r requirements.txt && python -m uvicorn api.main:app --port 8000",
);
process.exit(1);
