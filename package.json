{
  "name": "it-ticket-auto-resolution",
  "version": "0.2.0",
  "private": true,
  "description": "Intelligent IT Ticket Auto-Resolution System v0.2.0 (Python FastAPI + pgvector + Redis)",
  "scripts": {
    "install:deps": "pip install -r requirements.txt",
    "seed": "python scripts/seed_db.py",
    "serve": "python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload",
    "eval": "python scripts/eval.py",
    "demo": "python scripts/demo.py",
    "docker:up": "docker-compose up -d --build",
    "start": "npm run serve"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20",
    "python": ">=3.10"
  }
}
